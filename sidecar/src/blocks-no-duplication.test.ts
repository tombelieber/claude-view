import { describe, it } from 'vitest'

describe('blocks_snapshot deduplication regression', () => {
  it.todo('blocks_snapshot has no duplicate messageIds after connect/reconnect cycle')
  it.todo('assistant text appears exactly once in committed blocks')
})
