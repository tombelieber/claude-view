import { describe, it } from 'vitest'

describe('Multi-session isolation', () => {
  it.todo('2 WS connections receive only their own session events')
  it.todo('Ending session A does not affect session B')
})
