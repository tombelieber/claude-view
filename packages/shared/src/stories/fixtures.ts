/**
 * Re-export fixtures from the canonical location in apps/web.
 * Shared stories import from here; the real data lives in apps/web/src/stories/fixtures.ts.
 */
export * from '../../../../apps/web/src/stories/fixtures'
