import { test } from '@playwright/test'

test.describe('Chat V2 Dockview', () => {
  test.fixme('open session → send message → response appears in panel')
  test.fixme('split right → two panels visible')
  test.fixme('tab shows session status dot')
})
