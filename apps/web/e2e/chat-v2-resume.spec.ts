import { test } from '@playwright/test'

test.describe('Chat V2 Resume', () => {
  test.fixme('resume shows time separator between old and new messages')
  test.fixme('resumed session preserves previous conversation blocks')
})
