import { test } from '@playwright/test'

test.describe('Chat V2 Keyboard', () => {
  test.fixme('Ctrl+T opens new session tab')
  test.fixme('Ctrl+W triggers close confirmation on active session')
})
