import { test } from '@playwright/test'

test.describe('Chat V2 Takeover', () => {
  test.fixme('watching state shows Take Over button')
  test.fixme('Take Over button shows confirmation dialog')
  test.fixme('confirming takeover transitions to active state')
})
