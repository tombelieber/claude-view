import { describe, it } from 'vitest'

describe('ChatPageV2', () => {
  it.todo('renders ChatDockLayout when sidecar is connected')
  it.todo('routes /chat/:sessionId opens correct panel')
  it.todo('fetches session list from GET /api/sessions on mount')
})
