import { describe, it } from 'vitest'

describe('ChatStatusBar', () => {
  it.todo('displays model name from connection')
  it.todo('formats token count with commas (e.g. 45,231)')
  it.todo('shows context percentage')
  it.todo('shows cost when totalCost is not null')
  it.todo('hides cost section when totalCost is null')
})
