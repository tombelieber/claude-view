import { describe, it } from 'vitest'

describe('ChatPanel', () => {
  it.todo('renders ConversationThread with live blocks when connected')
  it.todo('renders ConversationThread with history blocks when disconnected')
  it.todo('renders ChatStatusBar with connection data')
  it.todo('renders ChatInputBar')
  it.todo('passes skip=true to useSidecarConnection when isWatching')
})
