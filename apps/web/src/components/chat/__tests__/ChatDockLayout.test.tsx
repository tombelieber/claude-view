import { describe, it } from 'vitest'

describe('ChatDockLayout', () => {
  it.todo('adds panel when session is activated from sidebar')
  it.todo('removes panel when session tab is closed')
  it.todo('split right creates second group with same session type')
  it.todo('restores layout from localStorage if saved')
})
