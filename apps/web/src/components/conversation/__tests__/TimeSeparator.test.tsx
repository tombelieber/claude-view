import { describe, it } from 'vitest'

describe('TimeSeparator', () => {
  it.todo('shouldShowSeparator returns false for gap < 30 minutes (in seconds)')
  it.todo('shouldShowSeparator returns true for gap > 30 minutes (in seconds)')
  it.todo('shouldShowSeparator returns false when timestamps are missing')
  it.todo('formatRelativeDate converts unix seconds to readable relative date')
  it.todo('renders separator line with formatted date')
})
