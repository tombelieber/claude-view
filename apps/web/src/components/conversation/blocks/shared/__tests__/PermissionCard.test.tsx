import { describe, it } from 'vitest'

describe('PermissionCard — hardening fixes', () => {
  it.todo('shows toast when permission countdown reaches 0 (Fix 1)')
  it.todo('disables buttons and shows spinner when isPending (Fix 2)')
  it.todo('renders "Always Allow" button when suggestions present (Fix 5)')
  it.todo('calls onAllow with updatedPermissions when "Always Allow" clicked')
})
