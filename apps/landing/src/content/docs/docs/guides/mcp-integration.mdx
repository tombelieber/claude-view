---
title: MCP Integration
description: Give Claude Code native tool access to your session data, costs, and fluency scores — via the claude-view plugin (coming soon).
---

import { MCP_TOOL_COUNT } from '../../../../data/site';

:::caution[Coming Soon]
The claude-view plugin is in development and not yet published. This page describes the planned integration. Check back soon or [watch the repo](https://github.com/tombelieber/claude-view) for the release.
:::

## Overview

claude-view will ship as a **Claude Code plugin** (`@claude-view/plugin`) that gives Claude Code native access to {MCP_TOOL_COUNT} session tools — no manual `settings.json` editing required.

Once published, installation will be a single command:

```bash
claude plugin add @claude-view/plugin
```

## Available tools

The plugin exposes {MCP_TOOL_COUNT} read-only tools:

| Tool | Description |
|------|-------------|
| `list_sessions` | Browse sessions with filters (project, model, date, status) |
| `get_session` | Session detail with messages, tool calls, and metrics |
| `search_sessions` | Full-text search across all conversations |
| `get_stats` | Dashboard overview — total sessions, costs, trends |
| `get_fluency_score` | AI Fluency Score (0–100) with breakdown |
| `get_token_stats` | Token usage with cache hit ratio |
| `list_live_sessions` | Currently running agents (real-time) |
| `get_live_summary` | Aggregate cost and status for today |

All tools are read-only. All data stays local — no external requests.

## Example usage (preview)

```
"How much have I spent on Claude Code today?"
→ Uses get_live_summary

"Find my last session about authentication"
→ Uses search_sessions(query: "authentication")

"What's my AI Fluency Score?"
→ Uses get_fluency_score
```

## Architecture

The plugin connects to the running claude-view server on `localhost:47892`. Start the server first:

```bash
npx claude-view
```
